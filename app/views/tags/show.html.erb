<h1>Articles Tagged with <%= @tag.name %></h1>

<ul>
  <% @tag.articles.each do |article| %>
      <h2><%= link_to article.title, article_path(article) %></h2>
  <% end %>
</ul>

<ul><h2> | <%= link_to 'Back', articles_path %> | </h2></ul>